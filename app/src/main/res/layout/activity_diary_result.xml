<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_diary_result"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFEEE0"
    tools:context=".DiaryResultActivity">

    <!-- 상단 로고 -->
    <LinearLayout
        android:id="@+id/layout_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:layout_margin="16dp"
        android:orientation="horizontal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <TextView
            android:id="@+id/app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="32sp"
            android:textColor="#5D3316"
            android:fontFamily="@font/paperlogy_7"
            />
        <ImageView
            android:id="@+id/img_logo"
            android:layout_width="52sp"
            android:layout_height="52sp"
            android:src="@drawable/photolog_logo"
            android:contentDescription="@string/app_logo_desc"
            android:layout_margin="2dp"
            />
    </LinearLayout>

    <!-- 다시하기 버튼 -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_retry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="25dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/button_background"
        android:fontFamily="@font/paperlogy_7"
        android:paddingHorizontal="14dp"
        android:text="다시하기"
        android:textColor="#5D3316"
        android:textSize="30sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <!-- 액자형 사진 (3:2 비율, 둥근 모서리, 내부 이미지도 라운드 적용) -->
    <FrameLayout
        android:id="@+id/photo_frame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="24dp"
        android:background="@drawable/photo_frame_background"
        app:layout_constraintTop_toBottomOf="@id/btn_retry"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="3:2">

        <ImageView
            android:id="@+id/iv_photo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:clipToOutline="true"
            android:background="@drawable/photo_clip_mask"
            tools:src="@drawable/sample" />
    </FrameLayout>

    <!-- 사진 아래 구분선 -->
    <View
        android:id="@+id/separator_mid"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:background="#C7A882"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/photo_frame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 일기 제목 + 날짜 + 수정하기 버튼 -->
    <LinearLayout
        android:id="@+id/layout_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@id/separator_mid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="일기 제목"
                android:layout_marginStart="10dp"
                android:layout_marginTop="10dp"
                android:textColor="#5D3316"
                android:textSize="24sp"
                android:fontFamily="@font/paperlogy_7" />

            <TextView
                android:id="@+id/tv_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0000년 00월 00일"
                android:textColor="#8C6B56"
                android:textSize="18sp"
                android:layout_marginStart="10dp"
                android:layout_marginTop="2dp"
                android:fontFamily="@font/paperlogy_6" />
        </LinearLayout>
        <CheckBox
            android:id="@+id/groupsee"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="그룹 공개"
            android:paddingRight="10dp"
            android:textSize="18dp"
            android:textColor="#5D3316"
            />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_edit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:background="@drawable/button_background"
            android:fontFamily="@font/paperlogy_7"
            android:paddingHorizontal="14dp"
            android:text="수정하기"
            android:textColor="#5D3316"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </LinearLayout>

    <!-- 일기 내용 박스 (사진과 동일한 비율로) -->
    <FrameLayout
        android:id="@+id/frame_content_box"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="6dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/edittext_background"
        app:layout_constraintTop_toBottomOf="@id/layout_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="4:3">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="10dp">

            <EditText
                android:id="@+id/et_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@null"
                android:padding="12dp"
                android:gravity="top|start"
                android:scrollbars="vertical"
                android:overScrollMode="always"
                android:text="AI가 생성한 일기 내용이 여기에 표시됩니다.\n\n예: 오늘은 강아지와 함께 산책을 나갔다. 하늘이 맑고 기분 좋은 하루였다..."
                android:textColor="#5D3316"
                android:textSize="18sp"
                android:lineSpacingExtra="4dp"
                android:fontFamily="@font/paperlogy_6"
                android:inputType="textMultiLine"
                />
        </ScrollView>
    </FrameLayout>

    <!-- 작성 완료 버튼 -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_done"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/button_background"
        android:fontFamily="@font/paperlogy_7"
        android:paddingVertical="5dp"
        android:clickable="true"
        android:focusable="true"
        android:text="작성 완료"
        android:textColor="#5D3316"
        android:textSize="34sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/frame_content_box"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
