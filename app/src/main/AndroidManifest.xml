<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Photolog"

        android:usesCleartextTraffic="true">   <!-- ⭐⭐ HTTP 통신 허용 추가 ⭐⭐ -->

        <!-- 회원가입 -->
        <activity android:name=".SignupActivity"
            android:exported="false"/>

        <!-- 챗봇 -->
        <activity android:name=".ChatbotActivity"
            android:exported="false"/>

        <!-- 일기 생성 -->
        <activity android:name=".DiaryGenerationActivity"
            android:exported="false"/>

        <!-- 메인 홈 -->
        <activity android:name=".MainActivity"
            android:exported="false"/>

        <!-- 로그인 (런처 Activity) -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 나머지 화면들 -->
        <activity android:name=".DiaryResultActivity"
            android:exported="false"/>
        <activity android:name=".FamilyDiaryActivity"
            android:exported="false"/>
        <activity android:name=".FamilyDiaryDetailActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>
        <activity android:name=".FindIdResultActivity"/>
        <activity android:name=".FindIdActivity" />
        <activity android:name=".FindPwdActivity"/>
        <activity android:name=".RePwdActivity"/>
        <activity android:name=".RePwdResultActivity"/>
        <activity android:name=".MyPageActivity"/>
        <activity android:name=".MakeGroupActivity"/>
        <activity android:name=".MakeGroupResponse" />
        <activity android:name=".DiaryViewActivity" />
        <activity android:name=".MyDiaryActivity" />

    </application>

</manifest>
